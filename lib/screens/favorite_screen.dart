import 'package:flutter/material.dart';

class FavoriteScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // Implementando tela pra exibir as refeições favorita
    return Container(
      child: Column(
        children: <Widget>[
          Text('Nenhuma refeição no momento!'),
        ],
      ),
    );
  }
}
